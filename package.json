{
  "name": "syzmeku-engine",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.cjs",
  "scripts": {
    "start": "node server/server.cjs",
    "setup": "npm cache clean --force && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run setup && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
