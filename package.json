// --- FILE: package.json (in the main project root) ---
// ... (rest of the file content)

"scripts": {
    "start": "node server/server.cjs",
    "setup": "npm install && cd client && npm install && cd .. && cd server && npm install",
    "client-build": "cd client && npm run build",
    "render-build": "npm run setup && npm run client-build"  // <-- CHANGED THIS LINE
},
"dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
},

// ... (rest of the file content)
