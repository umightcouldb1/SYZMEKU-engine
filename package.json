{
  "name": "syzmeku-engine",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.cjs",
  "scripts": {
    "start": "node server/server.cjs",
    "dev": "npm install && npm run install-all && npm run dev-server & npm run dev-client",
    "dev-server": "nodemon server/server.cjs",
    "dev-client": "cd client && npm run dev",
    "install-all": "npm install && cd client && npm install",
    "setup": "npm install && cd client && npm install",
    "client-build": "cd client && npm run build",
    "render-build": "npm install && npm run setup && npm run client-build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",  <-- ADD THIS LINE
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.2"
  }
}
